# リポジトリのガイドライン

## ファイル構成

- `configs/<category>/<feature>.yml` を、各再利用機能の公開インターフェースとして扱うこと。
- 実行可能なスクリプトは `.lefthook/<hook-name>/` に配置すること。

## 設定設計

- Lefthook の hook 設定は `jobs` 形式で記述し、`commands` や `scripts` 形式を使用しないこと。
- 1 つの共有機能が相互に依存しない複数の job を持ち、並列化が有効な場合は、それらを `group` にまとめて `parallel: true` を指定すること。
- 単一の job や並列化する必要がない job は、不要に `group` 化しないこと。
- 共有設定では hook レベルの `parallel` を指定しないこと。複数の共有機能をまたぐ並列化は利用側の設定に委ねること。
- 利用側が設定を個別に選択できるように、各機能を独立させること。
- 機能が明示的に必要とする場合を除き、利用側固有のツールや依存関係を追加しないこと。

## ドキュメント

- 動作、前提条件、運用上の注意事項は、設定 YAML 内のコメントとして記載すること。
- README のカタログは簡潔に保ち、各設定のパスと短い説明だけを記載すること。
- リポジトリ共通の導入手順と利用方法は、README で一度だけ説明すること。
- README、設定、スクリプト間で詳細な説明を重複させないこと。

## シェルスクリプト

- スクリプトの実行環境には Bash または PowerShell 7 (`pwsh`) のみを使用すること。
- Node.js、Python、Ruby など、Bash と `pwsh` 以外のスクリプトランタイムを使用しないこと。
- Bash スクリプトでは `set -euo pipefail` を使用すること。
- Git hook の引数と標準入力は、その hook の仕様に従って処理すること。
- 変数展開とパスは引用符で囲むこと。
- 操作を拒否する場合は、対処可能なエラーメッセージを標準エラー出力へ書き出し、0 以外の終了ステータスを返すこと。

## 検証

- 動作を変更する場合は、`tests/` 配下の統合テストを追加または更新すること。
- hook のテストは可能な限り `lefthook run` 経由で実行し、設定解決、引数、標準入力、スクリプト探索を含めて検証すること。
- 各機能について主要な許可ケースと拒否ケースを検証すること。複数入力を処理する場合は対象が後続に現れるケースを、前提条件がある場合は不足時のエラーを検証すること。
- 同じ制御経路を通る入力の組み合わせを網羅するのではなく、振る舞いを区別できる最小限のケースを選ぶこと。
- テストはネットワークへ接続せず、一時データを終了時に削除すること。
- 各テストの実行手順は `test.just` の個別 recipe として定義し、説明を記載すること。
- `test.just` の先頭 recipe は、すべてのテスト recipe に依存する `all` とすること。
- 設定検証やシェル構文検査も `test.just` の recipe を通して実行すること。
- 変更を完了する前に `just test` で全テストを実行すること。
- `.gitattributes` の改行コード規則に従うこと。
